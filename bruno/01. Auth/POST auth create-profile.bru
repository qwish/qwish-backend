meta {
  name: POST /auth/create-profile
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/create-profile
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "full_name":     "Alice Smith",
    "referral_code": "SINST-ABC"
  }
}
