meta {
  name: POST /quizzes/{quizId}/questions/{questionId}/reports
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/quizzes/:quizId/questions/:questionId/reports
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "reason":      "wrong_answer",
    "description": "Optional extra detail"
  }
}
