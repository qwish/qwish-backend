meta {
  name: POST /parent/link/{linkId}/accept
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/parent/link/:linkId/accept
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}
