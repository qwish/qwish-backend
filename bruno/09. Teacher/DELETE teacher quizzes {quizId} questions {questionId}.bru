meta {
  name: DELETE /teacher/quizzes/{quizId}/questions/{questionId}
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/teacher/quizzes/:quizId/questions/:questionId
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}
