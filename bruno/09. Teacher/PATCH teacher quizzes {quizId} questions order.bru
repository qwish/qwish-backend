meta {
  name: PATCH /teacher/quizzes/{quizId}/questions/order
  type: http
  seq: 14
}

patch {
  url: {{baseUrl}}/teacher/quizzes/:quizId/questions/order
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "order": ["question-uuid-1", "question-uuid-2", "question-uuid-3"]
  }
}
