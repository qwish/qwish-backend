meta {
  name: PATCH /teacher/quizzes/{quizId}/questions/{questionId}
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/teacher/quizzes/:quizId/questions/:questionId
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}
