meta {
  name: PATCH /institution/settings
  type: http
  seq: 24
}

patch {
  url: {{baseUrl}}/institution/settings
  body: json
  auth: none
}

body:json {
  { "name": "New Name", "timezone": "Europe/London", "type": "university" }
}
