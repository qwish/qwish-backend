meta {
  name: PATCH /institution/students/{userId}/status
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/institution/students/:userId/status
  body: json
  auth: none
}

body:json {
  { "action": "suspend", "reason": "Academic misconduct" }
}
