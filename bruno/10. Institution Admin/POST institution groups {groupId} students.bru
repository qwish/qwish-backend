meta {
  name: POST /institution/groups/{groupId}/students
  type: http
  seq: 14
}

post {
  url: {{baseUrl}}/institution/groups/:groupId/students
  body: json
  auth: none
}

body:json {
  { "user_id": "uuid" }
}
