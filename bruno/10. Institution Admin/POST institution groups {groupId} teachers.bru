meta {
  name: POST /institution/groups/{groupId}/teachers
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/institution/groups/:groupId/teachers
  body: json
  auth: none
}

body:json {
  { "user_id": "uuid" }
}
