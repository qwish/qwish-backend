meta {
  name: PATCH /admin/announcements/{announcementId}/retract
  type: http
  seq: 31
}

patch {
  url: {{baseUrl}}/admin/announcements/:announcementId/retract
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}
