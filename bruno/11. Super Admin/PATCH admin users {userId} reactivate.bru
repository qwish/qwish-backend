meta {
  name: PATCH /admin/users/{userId}/reactivate
  type: http
  seq: 15
}

patch {
  url: {{baseUrl}}/admin/users/:userId/reactivate
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}
