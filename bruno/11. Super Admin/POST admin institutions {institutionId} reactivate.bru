meta {
  name: POST /admin/institutions/{institutionId}/reactivate
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/admin/institutions/:institutionId/reactivate
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}
