meta {
  name: POST /admin/institutions/{institutionId}/reset-referral-codes
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/admin/institutions/:institutionId/reset-referral-codes
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}
