meta {
  name: POST /admin/institutions/{institutionId}/suspend
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/admin/institutions/:institutionId/suspend
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  { "reason": "Policy violation" }
}
