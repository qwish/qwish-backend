meta {
  name: POST /admin/quizzes/{quizId}/reject
  type: http
  seq: 23
}

post {
  url: {{baseUrl}}/admin/quizzes/:quizId/reject
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  { "reason": "Incorrect answers detected" }
}
