meta {
  name: POST /admin/users/{userId}/reset-password
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/admin/users/:userId/reset-password
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}
